# Konfigurationsvariablen für ScanOp
# Kopieren Sie diese Datei zu .env und füllen Sie die Werte aus.

# URL zur SQLAlchemy-Datenbank. Für SQLite (Standard):
DATABASE_URL="sqlite:///./scanop.db"

# --- NEU: Zugriffssteuerung ---
# Ein langer, zufälliger String, der zum Signieren von Session-Cookies verwendet wird.
# Ändern Sie diesen Wert unbedingt! Sie können z.B. `openssl rand -hex 32` verwenden.
SECRET_KEY="Ihr_super_geheimer_und_zufaelliger_schluessel_hier_einfuegen"

# Geheimer API-Schlüssel für die Client-Server-Kommunikation.
# Diesen Wert an die Clients verteilen.
SERVER_API_KEY="Ihr_sehr_langer_und_zufaelliger_api_schluessel_hier"

# Anmeldedaten für das Web-Interface
APP_USERNAME="admin"
# Das Klartext-Passwort. Es wird NICHT direkt verwendet.
# Siehe hash_password.py, um den Hash für die nächste Variable zu generieren.
APP_PASSWORD="changeme"